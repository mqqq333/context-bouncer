I've read all the key files. Now I have a complete picture of the `daily-paper-v1` fixture. Let me deliver the review.  ---  ## Scoped Review: `benchmarks/fixtures/daily-paper-v1`  Reviewed files: - `benchmarks/fixtures/daily-paper-v1/README.md` - `benchmarks/fixtures/daily-paper-v1/input/` (all 6 required files) - `benchmarks/fixtures/daily-paper-v1/expected/expected-review-judgment.md` - `benchmarks/fixtures/daily-paper-v1/expected/required-fixes.md` - `scripts/prepare_benchmark.py` (fixture consumer) - `benchmarks/templates/quality-rubric.md` - `benchmarks/templates/run-log-template.md` - `tests/test_context_bouncer.py`  ---  ### Issues  **Issue:** `prepare_benchmark.py` calls `validate_fixture()` which checks for `expected/expected-review-judgment.md` and `expected/required-fixes.md`, but there is no test that runs `prepare_benchmark.py` against the actual fixture — so fixture file-set regressions (e.g. a rename) won't be caught until a human tries to run the benchmark. **Severity:** Medium **Suggestion:** Add one test in `tests/` that calls `prepare_benchmark.main(["--fixture", str(FIXTURE_DIR), "--out", str(tmp)])` and asserts it exits 0 and produces the three arm prompts. This is the smallest check that fails if a required fixture file goes missing.  ---  **Issue:** `article.html` contains a hard-coded Windows absolute path `data-local-path="E:\private\daily-paper\cover.png"` which is the leakage the fixture is designed to detect — but there is no assertion anywhere that the *fixture itself* preserves this attribute across edits. If a future contributor "helpfully" cleans it up, the leakage-detection criterion silently becomes untestable. **Severity:** Medium **Suggestion:** Add a brief comment in `article.html` (e.g. `<!-- intentional: leakage fixture, do not remove data-local-path -->`) and a one-liner test that asserts `data-local-path` is still present in the raw fixture input file.  ---  **Issue:** `cover-generation-log.md` says `Status: generated` but there is no corresponding assertion in `expected/required-fixes.md` or the rubric that the fixture's status field must read exactly `generated` (vs. `complete`, `done`, etc.). An agent that grepped for a different word would pass the rubric criterion even if the fixture were later edited to an ambiguous value. **Severity:** Low **Suggestion:** Either pin the exact string in a comment in the log file (`<!-- fixture contract: "Status: generated" must stay as-is -->`), or add a sentence to `expected/required-fixes.md` noting the authoritative evidence string.  ---  **Issue:** The `figure-index.md` file is listed in `REQUIRED_INPUTS` (so `validate_fixture` checks for it and all three arm prompts reference it) but it is not referenced in `expected/expected-review-judgment.md` or `expected/required-fixes.md`. The rubric criterion for "Avoids unnecessary full PDF/material/log loading" would naturally use `figure-index.md` as evidence, but it's currently implicit. **Severity:** Low **Suggestion:** Add one line to `expected/expected-review-judgment.md` or `required-fixes.md` noting that `figure-index.md` confirms cover status and does not require loading the full PDF/source material.  ---  **Issue:** `prepare_benchmark.py`'s `build_prompt()` computes relative paths via `rel(fixture / 'input' / name, out_dir)`, but `out_dir` is the generated-run directory (gitignored), not the repo root. On a clean clone the relative paths in the generated prompts will be correct, but on any machine where `--out` resolves outside the repo the paths in the prompt become absolute or broken. This is a docs gap rather than a code bug, but it can confuse first-time runners. **Severity:** Low **Suggestion:** Add a one-line note to `benchmarks/README.md` (or the `--out` help string) saying the output directory should be under the repo root so relative fixture paths resolve correctly.  ---  No critical issues found. The fixture content is internally consistent: the leakage in `article.html` matches `expected-review-judgment.md`, the cover-generation log correctly says "generated" and maps to the "do not regenerate" instruction, the speculative review items in `claude-review-old.md` cleanly pair with the rejection list in the expected file, and the quality rubric criteria are all testable from the fixture inputs. The main gap is test coverage of the fixture itself.