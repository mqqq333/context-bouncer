# Fixture inputs

Execution agents may read these files. They must not read `../expected/` during execution.

The fixture intentionally contains large synthetic bloat in `article.html`, `claude-review-old.md`, and `figure-index.md`.
