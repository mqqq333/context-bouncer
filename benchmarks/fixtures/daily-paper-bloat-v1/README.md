# daily-paper-bloat-v1 fixture

Synthetic public-safe bloat fixture for Context Bouncer. It intentionally includes large review/log/HTML artifacts so long-session and whole-package review behavior have measurable context overhead.

This fixture is for usage/cost benchmarking only after provider-reported usage is imported. Do not claim savings from rough token estimates.
